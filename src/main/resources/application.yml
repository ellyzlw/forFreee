server:
  port: 8192
  address: 0.0.0.0

# database
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  web:
    resources:
      static-locations: classpath:/
  #redis:
  #  database: 0
  #  host: localhost
  #  port: 6379
  #session:
  #  store-type: redis
  #  timeout: 600s
  #thymeleaf:
  #  cache: false
  #  encoding: UTF-8
  #  mode: HTML
  #  suffix: .html
  #  prefix: classpath:/templates/

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  #configuration:
  #  map-underscore-to-camel-case: true
  #  default-fetch-size: 100
  #  default-statement-timeout: 30

#mybatis-plus:
#  configuration:
#    map-underscore-to-camel-case: true
